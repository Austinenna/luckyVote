# 服务器端口配置
PORT=3001

# 数据文件路径（可选，默认为项目根目录下的vote-data.json）
# DATA_FILE_PATH=./vote-data.json

# 开发环境配置
NODE_ENV=development

# CORS 配置（可选）
# CORS_ORIGIN=http://localhost:3000

# 日志级别（可选）
# LOG_LEVEL=info